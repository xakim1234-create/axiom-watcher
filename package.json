{
  "name": "axiom-watcher",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx puppeteer browsers install chrome --cache-dir=/opt/render/.cache/puppeteer || true"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "puppeteer": "^23.0.2"
  }
}
